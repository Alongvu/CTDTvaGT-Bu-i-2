// Hàm heapSort để sắp xếp mảng theo thứ tự từ lớn đến nhỏ
void heapSort(int arr[], int n) { 
    // Tạo cây heap từ mảng
    for (int i = n / 2 - 1; i >= 0; i--) {
        heapify(arr, n, i); }
 // Đổi chỗ phần tử đầu tiên của mảng (nút lớn nhất) với phần tử cuối cùng của mảng, giảm kích thước của mảng đi 1 và tiếp tục xử lý cây heap
    for (int i = n - 1; i >= 0; i--) {
        int temp = arr[0];
        arr[0] = arr[i];
        arr[i] = temp;
        heapify(arr, i, 0); } } }
